# Las dependencias Helm (.tgz) se commitean intencionalmente.
# ArgoCD las necesita en el repo para poder renderizar el chart sin acceso externo.
# Para regenerarlas localmente:
#   helm dependency update charts/pedido-app/charts/db
#   helm dependency update charts/pedido-app
